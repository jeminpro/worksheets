---
import type { MinuendPair } from "./types";

interface Props {
  pair: MinuendPair;
  index: number;
}

const { pair, index } = Astro.props;
const [top, bottom] = pair;
const answer = top - bottom;
---

<article class="problem" aria-label={`Question ${index + 1}: ${top} minus ${bottom}`}>
  <span class="problem-number">{index + 1}</span>
  <div class="operation">
    <div class="borrow-space" aria-hidden="true"></div>
    <div>{top}</div>
    <div class="subtrahend"><span class="minus">−</span><span>{bottom}</span></div>
    <div class="answer-rule"></div>
    <div class="answer" aria-hidden="true">{answer}</div>
  </div>
  <div class="working-space" aria-hidden="true"></div>
</article>

<style>
  .problem {
    position: relative;
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
    justify-items: end;
    min-width: 0;
    min-height: 0;
    padding: 2.4mm 3mm 2mm;
    border: 0.35mm solid #c7d7e1;
    border-radius: 3mm;
    background: linear-gradient(180deg, #fff 0%, #fff 73%, #f8fbfd 100%);
    overflow: hidden;
  }

  .problem-number {
    position: absolute;
    top: 2mm;
    left: 2.3mm;
    color: var(--blue-strong);
    font-size: 6.5pt;
    font-weight: 800;
  }

  .operation {
    position: relative;
    width: 28mm;
    margin: 0 0 1.6mm;
    color: #152b3d;
    font-family: "Arial Rounded MT Bold", "Trebuchet MS", Arial, sans-serif;
    font-size: 14pt;
    font-weight: 800;
    line-height: 1.02;
    text-align: right;
  }

  .borrow-space {
    height: 4.6mm;
    margin-bottom: 0.6mm;
  }

  .subtrahend {
    display: flex;
    justify-content: flex-end;
    gap: 1.3mm;
    padding-top: 0.4mm;
  }

  .minus {
    color: var(--peach-strong);
  }

  .answer-rule {
    height: 0;
    border-top: 0.65mm solid var(--ink);
  }

  .answer {
    position: absolute;
    top: 100%;
    right: 0;
    margin-top: 0.8mm;
    padding-left: 1.2mm;
    background: var(--paper);
    color: var(--blue-strong);
    visibility: hidden;
  }

  :global(.answers-visible) .answer {
    visibility: visible;
  }

  .working-space {
    width: 100%;
    min-height: 0;
  }
</style>
